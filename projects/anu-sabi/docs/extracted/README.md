# Extracted Modules

> Reserved for future standalone packages extracted from Anu-Sabi (e.g. Rambler Engine).

**Current status:** No extracted modules exist. Application code remains at repository root (`src/`).

**TBD — not in repository** — Documentation will be updated when a package is extracted.
